apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ourlib-app
  namespace: kps
spec:
  selector:
    matchLabels:
      app: ourlib
      namespace: ourlib
  endpoints:
    - port: web
      path: /metrics
